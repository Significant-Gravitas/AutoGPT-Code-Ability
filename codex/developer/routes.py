from fastapi import APIRouter
from codex.common import logging

logger = logging.getLogger(__name__)

developer_routes = APIRouter()

# Placeholder file in case we decide to add developer specific api routes
