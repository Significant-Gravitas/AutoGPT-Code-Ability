Create the a fully working a single page NiceGUI front-end application, with a main component called: {{ function_name }}

{% if function_signature %}
With this function signature:
```python
{{ function_signature }}
```
The created function has to match exactly as this signature!
{% endif %}

This function will be an entry function for a single page front-end application that you are going to create.
The goal is to create fully working, responsive, beatiful, and user-friendly front-end application that will be used to achieve this goal:
"{{ goal }}".

NOTE: IMPLEMENT THE REQUIRED FUNCTION WITH A REAL CODE IMPLEMENTATION, NOT JUST A STUB, PLACEHOLDER, OR PSEUDOCODE!
{% if provided_functions %}
----
You have already implemented these function components that is already available in the code, you can use it in your function, and no need to re-implement it. Here are the provided functions that you can use:
```python
{% for function in provided_functions %}
{{ function }}
{% endfor %}
```

{% if database_schema %}
This is the list of functions that you can use as a datasource:

```
{{ database_schema }}
```
Only use these to interract with the datasource.

{% endif %}


NOTE:
DO NOT RE-WRITE THESE FUNCTIONS & CLASSES. JUST CALL IT IN THE FUNCTION THAT YOU WRITE (IF NEEDED).
YOU SHOULD ONLY PRODUCE THE REQUIRED FUNCTION AND THE NEW STUBS (IF THERE IS ANY).
----
{% endif %}
